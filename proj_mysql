CREATE TABLE grades (
    id INT AUTO_INCREMENT PRIMARY KEY,
    grade VARCHAR(1),
    min_score INT,
    max_score INT
);

INSERT INTO grades (grade, min_score, max_score)
VALUES
    ('S', 80, 100),
    ('A', 70, 80),
    ('B', 60, 70),
    ('C', 50, 60),
    ('D', 40, 50),
    ('F', 0, 40);
